spring:
  profiles:
    active: local
  config:
    import: "optional:classpath:/${spring.profiles.active}/yml/application-${spring.profiles.active}.yml"

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG