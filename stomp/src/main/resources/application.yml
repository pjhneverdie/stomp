spring:
  profiles:
    active: local
  config:
    import: "optional:classpath:/yml/${spring.profiles.active}/application-${spring.profiles.active}.yml"

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG